-- ~/.config/hypr/hyprland.lua
-- CachyOS Hyprland Main Lua Configuration

-- Load essential sub-modules
require("config.variables")     -- Global variables, gaps, active apps
require("config.colors")        -- Color definitions
require("config.decorations")   -- Styling, borders, gaps, shadows, blur
require("config.animations")    -- Animations and curves
require("config.misc")          -- Miscellaneous options
require("config.monitors")      -- Layouts and screen scaling
require("config.inputs")        -- Keyboard, mouse, and trackpad layouts
require("config.binds")         -- System keybindings and hotkeys
require("config.windowrules")   -- Floating window rules and behavior
require("config.workspaces")    -- Workspace assignments and monitor anchors
require("config.environment")   -- System and graphics env variables
require("config.autostart")     -- Autostart programs and services
